{
  "name": "open-abap-ci",
  "version": "1.0.0",
  "private": true,
  "description": "test",
  "scripts": {
    "unit": "rm -rf output && abap_transpile && echo RUNNING && node output/index.mjs",
    "test": "abaplint && npm run unit"
  },
  "license": "",
  "dependencies": {
    "@abaplint/cli": "^2.119.12",
    "@abaplint/runtime": "^2.13.18",
    "@abaplint/database-sqlite": "^2.11.78",
    "@abaplint/transpiler-cli": "^2.13.18"
  }
}
